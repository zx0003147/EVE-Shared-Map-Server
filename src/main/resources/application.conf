# Runtime configuration is loaded and validated by dev.evesharedmap.server.config.ServerConfig.
# This resource is retained as the Ktor configuration boundary for future protocol phases.
sharedMap {
    entryPoint = "dev.evesharedmap.server.MainKt"
}
